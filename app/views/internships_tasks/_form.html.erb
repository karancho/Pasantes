<%= form_for(@internships_task) do |f| %>
  <% if @internships_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@internships_task.errors.count, "error") %> prohibited this internships_task from being saved:</h2>

      <ul>
      <% @internships_task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :internship_id %><br />
    <%= f.number_field :internship_id %>
  </div>
  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.number_field :task_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

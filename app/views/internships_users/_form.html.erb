<%= form_for(@internships_user) do |f| %>
  <% if @internships_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@internships_user.errors.count, "error") %> prohibited this internships_user from being saved:</h2>

      <ul>
      <% @internships_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :internship_id %><br />
    <%= f.number_field :internship_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
